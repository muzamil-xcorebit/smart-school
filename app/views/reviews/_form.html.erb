<%= form_for([@pod, @pod.reviews.build], remote: true) do |f| %>
  <br>
  <%= f.text_area :review , required: true, placeholder: "Review" %><br>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
